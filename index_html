<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>מבוא למערכות מידע וניהול נתונים - קורס מקיף</title>
    <link href="https://fonts.googleapis.com/css2?family=Assistant:wght@300;400;600;700&family=Heebo:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #2c3e50;
            --secondary-color: #3498db;
            --accent-color: #e74c3c;
            --light-color: #ecf0f1;
            --dark-color: #2c3e50;
            --success-color: #27ae60;
            --warning-color: #f39c12;
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Assistant', sans-serif;
            line-height: 1.6;
            color: #333;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Header Styles */
        header {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            box-shadow: 0 2px 20px rgba(0, 0, 0, 0.1);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 0;
        }

        .logo h1 {
            font-family: 'Heebo', sans-serif;
            color: var(--primary-color);
            font-size: 1.8rem;
            font-weight: 700;
        }

        nav ul {
            display: flex;
            list-style: none;
            gap: 2rem;
        }

        nav a {
            text-decoration: none;
            color: var(--primary-color);
            font-weight: 600;
            transition: var(--transition);
            padding: 0.5rem 1rem;
            border-radius: 25px;
        }

        nav a:hover {
            background: var(--secondary-color);
            color: white;
            transform: translateY(-2px);
        }

        /* Hero Section */
        .hero {
            text-align: center;
            padding: 5rem 0;
            background: linear-gradient(rgba(44, 62, 80, 0.8), rgba(44, 62, 80, 0.9)), url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><rect width="100" height="100" fill="%233498db"/><circle cx="20" cy="20" r="5" fill="%232c3e50" opacity="0.3"/><circle cx="80" cy="40" r="3" fill="%232c3e50" opacity="0.3"/><circle cx="40" cy="80" r="4" fill="%232c3e50" opacity="0.3"/></svg>');
            color: white;
            margin-bottom: 3rem;
        }

        .hero h2 {
            font-size: 3rem;
            margin-bottom: 1rem;
            font-family: 'Heebo', sans-serif;
        }

        .hero p {
            font-size: 1.2rem;
            max-width: 700px;
            margin: 0 auto 2rem;
        }

        .cta-button {
            background: var(--accent-color);
            color: white;
            padding: 1rem 2rem;
            border: none;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: var(--transition);
            text-decoration: none;
            display: inline-block;
        }

        .cta-button:hover {
            background: #c0392b;
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(231, 76, 60, 0.3);
        }

        /* Sections */
        section {
            background: white;
            margin: 2rem 0;
            padding: 3rem 0;
            border-radius: 15px;
            box-shadow: 0 5px 30px rgba(0, 0, 0, 0.1);
        }

        .section-title {
            text-align: center;
            color: var(--primary-color);
            font-family: 'Heebo', sans-serif;
            margin-bottom: 2rem;
            font-size: 2rem;
        }

        .content-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            padding: 0 2rem;
        }

        .card {
            background: var(--light-color);
            padding: 2rem;
            border-radius: 15px;
            transition: var(--transition);
            border: 2px solid transparent;
        }

        .card:hover {
            transform: translateY(-5px);
            border-color: var(--secondary-color);
            box-shadow: 0 15px 35px rgba(52, 152, 219, 0.2);
        }

        .card h3 {
            color: var(--primary-color);
            margin-bottom: 1rem;
            font-family: 'Heebo', sans-serif;
        }

        .card-icon {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            color: var(--secondary-color);
        }

        /* Trends Section */
        .trends {
            background: linear-gradient(135deg, #3498db 0%, #2c3e50 100%);
            color: white;
        }

        .trends .section-title {
            color: white;
        }

        .trend-item {
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .trend-item h3 {
            color: white;
        }

        /* Contact Section */
        .contact-form {
            max-width: 600px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 600;
            color: var(--primary-color);
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 1rem;
            border: 2px solid #ddd;
            border-radius: 10px;
            font-family: 'Assistant', sans-serif;
            font-size: 1rem;
            transition: var(--transition);
        }

        .form-group input:focus,
        .form-group textarea:focus {
            border-color: var(--secondary-color);
            outline: none;
            box-shadow: 0 0 10px rgba(52, 152, 219, 0.3);
        }

        .submit-btn {
            background: var(--success-color);
            color: white;
            padding: 1rem 2rem;
            border: none;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: var(--transition);
            width: 100%;
        }

        .submit-btn:hover {
            background: #219a52;
            transform: translateY(-2px);
        }

        /* Footer */
        footer {
            background: var(--dark-color);
            color: white;
            text-align: center;
            padding: 2rem 0;
            margin-top: 3rem;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .header-content {
                flex-direction: column;
                gap: 1rem;
            }

            nav ul {
                flex-wrap: wrap;
                justify-content: center;
                gap: 1rem;
            }

            .hero h2 {
                font-size: 2rem;
            }

            .content-grid {
                grid-template-columns: 1fr;
            }
        }

        /* Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .animated {
            animation: fadeInUp 0.6s ease forwards;
        }

        .delay-1 { animation-delay: 0.1s; }
        .delay-2 { animation-delay: 0.2s; }
        .delay-3 { animation-delay: 0.3s; }
        .delay-4 { animation-delay: 0.4s; }
    </style>
</head>
<body>
    <!-- Header -->
    <header>
        <div class="container header-content">
            <div class="logo">
                <h1>מבוא למערכות מידע וניהול נתונים</h1>
            </div>
            <nav>
                <ul>
                    <li><a href="#about">על הקורס</a></li>
                    <li><a href="#modules">מודולים</a></li>
                    <li><a href="#trends">מגמות מתקדמות</a></li>
                    <li><a href="#instructor">המדריך</a></li>
                    <li><a href="#contact">הרשמה</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero">
        <div class="container">
            <h2>התחל את המסע בעולם大数据!</h2>
            <p>קורס מקיף שמלמד אותך את היסודות וה先锋 של מערכות מידע וניהול נתונים, כולל Machine Learning, Cloud Computing, ו-Big Data Analytics</p>
            <a href="#contact" class="cta-button animated">הרשמה מוקדמת</a>
        </div>
    </section>

    <!-- About Section -->
    <section id="about">
        <div class="container">
            <h2 class="section-title animated">על הקורס</h2>
            <div class="content-grid">
                <div class="card animated delay-1">
                    <div class="card-icon">📊</div>
                    <h3>בסיסים חזקים</h3>
                    <p>לימוד יסודות מערכות מידע, מבנה נתונים, אלגוריתמים, וניהול מסדי נתונים רלציוניים.</p>
                </div>
                <div class="card animated delay-2">
                    <div class="card-icon">🚀</div>
                    <h3>טכנולוגיות מתקדמות</h3>
                    <p>חשיפה לטכנולוגיות cutting-edge כמו NoSQL, Cloud Databases, ו-Analytics Platforms.</p>
                </div>
                <div class="card animated delay-3">
                    <div class="card-icon">💡</div>
                    <h3> חשיבה אנליטית</h3>
                    <p>פיתוח יכולות ניתוח נתונים,可视化, והפקת תובנות עסקיים מה(datos.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Modules Section -->
    <section id="modules">
        <div class="container">
            <h2 class="section-title animated">מודולי הלימוד</h2>
            <div class="content-grid">
                <div class="card animated delay-1">
                    <h3>مدخل لقواعد البيانات</h3>
                    <p>SQL Avanzado, تصميم قواعد البيانات, النماذج المختلفة, والأداء الأمثل.</p>
                </div>
                <div class="card animated delay-2">
                    <h3>برمجة البيانات</h3>
                    <p>Python للبيانات، Pandas، NumPy، والتعامل مع مصادر البيانات المختلفة.</p>
                </div>
                <div class="card animated delay-3">
                    <h3>تحليل البيانات</h3>
                    <p>احصائيات وصفية، نماذج تنبؤية، وتصور البيانات باستخدام Matplotlib و Seaborn.</p>
                </div>
                <div class="card animated delay-4">
                    <h3>Big Data و Cloud</h3>
                    <p>Hadoop, Spark, AWS, Google Cloud Platform, وتقنيات معالجة البيانات الضخمة.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Trends Section -->
    <section id="trends" class="trends">
        <div class="container">
            <h2 class="section-title animated">מגמות מתקדמות בתחום</h2>
            <div class="content-grid">
                <div class="card trend-item animated delay-1">
                    <div class="card-icon">🤖</div>
                    <h3>AI ו-Machine Learning</h3>
                    <p>אינטגרציה של AI במערכות מידע, Deep Learning, Neural Networks, ו-AutoML.</p>
                </div>
                <div class="card trend-item animated delay-2">
                    <div class="card-icon">☁️</div>
                    <h3>Cloud Computing</h3>
                    <p>שירותי ענן, Serverless Architecture, Microservices, ו-Scale-out Solutions.</p>
                </div>
                <div class="card trend-item animated delay-3">
                    <div class="card-icon">🔒</div>
                    <h3>אבטחת מידע ו-Cybersecurity</h3>
                    <p>Data Privacy, GDPR, Encryption, ו-Governance of Data.</p>
                </div>
                <div class="card trend-item animated delay-4">
                    <div class="card-icon">🔄</div>
                    <h3>Real-time Data Processing</h3>
                    <p>Stream Processing, Event-Driven Architecture, ו-IoT Data Integration.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Instructor Section -->
    <section id="instructor">
        <div class="container">
            <h2 class="section-title animated">המדריך המקצועי</h2>
            <div class="content-grid">
                <div class="card animated delay-1">
                    <div class="card-icon">👨‍🏫</div>
                    <h3>דר' דני כהן</h3>
                    <p>מומחה מוביל בתחום מערכות מידע עם מעל 15 שנות ניסיון בתעשייה ובאקדמיה. בוגר MIT וมหา"ת תל אביב.</p>
                    <p> ранее عمل في شركة Google و IBM كخبير بيانات رئيسي.</p>
                </div>
                <div class="card animated delay-2">
                    <div class="card-icon">🎓</div>
                    <h3>גישה ייחודית</h3>
                    <p>שילוב בין תיאוריה לפרקטיקה, פרויקטים אמיתיים, והדרכה אישית לכל סטודנט.</p>
                    <p>استخدام تقنيات التعلم الحديثة مثل Gamification والتعلم المعكوس.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section id="contact">
        <div class="container">
            <h2 class="section-title animated">הרשמה לקורס</h2>
            <div class="contact-form">
                <div class="form-group animated delay-1">
                    <label for="name">שם מלא</label>
                    <input type="text" id="name" placeholder="הכנס את שמך המלא">
                </div>
                <div class="form-group animated delay-2">
                    <label for="email">כתובת אימייל</label>
                    <input type="email" id="email" placeholder="הכנס את כתובת האימייל שלך">
                </div>
                <div class="form-group animated delay-3">
                    <label for="phone">מספר טלפון</label>
                    <input type="tel" id="phone" placeholder="הכנס את מספר הטלפון שלך">
                </div>
                <div class="form-group animated delay-4">
                    <label for="message">ทำไม אתה מעוניין בקורס?</label>
                    <textarea id="message" rows="4" placeholder="ספר לנו קצת על עצמך ועל תחומי העניין שלך"></textarea>
                </div>
                <button type="submit" class="submit-btn animated delay-5">שלח בקשה להרשמה</button>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <p>&copy; 2024 מבוא למערכות מידע וניהול נתונים. כל הזכויות שמורות.</p>
            <p>נוצר עם ❤️ לעתיד הבהיר של טכנולוגיית המידע</p>
        </div>
    </footer>

    <script>
        // Smooth scrolling for navigation links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Animation on scroll
        const observerOptions = {
            root: null,
            rootMargin: '0px',
            threshold: 0.1
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('animated');
                }
            });
        }, observerOptions);

        // Observe elements for animation
        document.querySelectorAll('.card, .section-title').forEach(el => {
            observer.observe(el);
        });

        // Form submission handling
        const contactForm = document.querySelector('#contact form') || document.querySelector('.contact-form');
        if (contactForm) {
            contactForm.addEventListener('submit', function(e) {
                e.preventDefault();
                
                // Get form values
                const name = document.getElementById('name')?.value;
                const email = document.getElementById('email')?.value;
                
                if (name && email) {
                    alert(`תודה ${name}! בקשת ההרשמה שלך נשלחה. נחזור אליך ב breve לכתובת ${email}`);
                    
                    // Reset form
                    this.reset();
                } else {
                    alert('אנא מלא את כל השדות הנדרשים');
                }
            });
        }

        // Header scroll effect
        window.addEventListener('scroll', () => {
            const header = document.querySelector('header');
            if (window.scrollY > 100) {
                header.style.boxShadow = '0 5px 20px rgba(0, 0, 0, 0.2)';
                header.style.background = 'rgba(255, 255, 255, 0.98)';
            } else {
                header.style.boxShadow = '0 2px 20px rgba(0, 0, 0, 0.1)';
                header.style.background = 'rgba(255, 255, 255, 0.95)';
            }
        });
    </script>
</body>
</html>
